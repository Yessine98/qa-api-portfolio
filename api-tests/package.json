{
  "name": "qa-api-portfolio-reqres-tests",
  "version": "1.0.0",
  "description": "Automated API tests for ReqRes using Mocha + Chai + Supertest (CommonJS).",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "test": "mocha \"test/**/*.spec.js\" --timeout 10000 --reporter spec"
  },
  "dependencies": {
    "chai": "^4.4.1",
    "dotenv": "^16.4.5",
    "supertest": "^6.3.4"
  },
  "devDependencies": {
    "mocha": "^10.3.0"
  }
}
